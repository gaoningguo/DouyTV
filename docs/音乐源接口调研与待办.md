# 音乐源接口清单 · 调用矩阵 · 扩展落点

> 调研 2026-06-21,读穿 5 个项目源码(`D:\工作目录\本机\Rust\`)。
> **最后更新 2026-06-27**:对照源码逐项重新核验,调用矩阵刷新到真实现状(此前一大批 ⬜ 已接入)。
> 本文回答三件事:① 各项目提供的全部接口;② 我们项目(`src/lib/music/`)调了哪些;③ 没调的能用在哪。
> 图例:✅=我们已调用 · ⬜=未调用 · 🔑=需登录 cookie · ⭐=enhanced 独占
>
> ## 2026-06-27 核验结论(先读这段)
>
> 五源逐项对照源码核验完成,**核心 + 发现页 + 扩展接口已基本全覆盖**,本轮发现并修复 3 个 bug:
> 1. ✅ **OmniParse netease `/song` 原是 GET `?id=&quality=` → 已改 POST `{ids,level,type}`**(`cyreneApi.ts` `buildOmniRequest`,源码 `index.ts` L85-127 确证)。QQ/酷狗分支本就是 GET,正确。
> 2. ✅ **`getNeteaseSongDetail` builtin 的 `c` 参数原传字符串 id → 已改数字 id**(贴齐 `song_detail.js`)。
> 3. ✅ **`getNeteaseDjCategoryRecommend` 命名误导 → 已改名 `getNeteaseDjProgramRecommend`**(端点 `/personalized/djprogram` 本就是「推荐电台节目」,功能正确,仅函数名误导)。
>
> 真正仍未接入的只剩:**登录态接口**(每日推荐/私人FM/红心,需 cookie)、**B站音源**(OmniParse `/bili/*`)、**Subsonic 协议**、**洛雪 WS 歌单同步**、**UNM 自建 wrapper**(已被 enhanced `/song/url/match` 覆盖,无需自建)。详见第 7 节。

## 五源速查总览

| 源 | 形态 | 接口规模 | 我们对接 | 健康度 |
|---|---|---|---|---|
| 网易云 API(NCM / enhanced) | REST | 377 / 399 个 module | `neteaseApi.ts`(builtin+external) | ✅ 核心+发现页+解灰+歌手/专辑/电台/MV/首页全通,缺登录态接口 |
| OmniParse | REST(bun) | 19 路由 | `cyreneApi.ts` omni 模式 | ✅ 搜索/播放/榜单/抖音已通(netease `/song` POST 已修),缺 B站音源 |
| UnblockNeteaseMusic | MITM 代理 + `match()` 库,**无 REST** | — | 未对接(能力被 enhanced `/song/url/match` 覆盖) | — 无需自建 wrapper |
| lxserver 增强 fork | REST + WS + Subsonic + web 播放器 | 100+ 路由 | `lxServer.ts`+`discovery.ts` | ✅ 播放+歌手/专辑/评论/歌单搜全通,缺 WS 同步/Subsonic |
| TuneHub V3 | REST(商业 credit 计费) | `/v1/parse` + `/v1/methods/*` | `cyreneApi.ts` tunehub 模式 | ✅ `POST /v1/parse`+`X-API-Key` 已实现(旧 `?type=url` 已弃) |

> 核实方式:网易/OmniParse/UNM/lxserver 均读源码入口逐行确认;TuneHub 闭源,经本机代理 `127.0.0.1:7777` curl 实测端点 + 抓 SPA 文档 chunk。

## 调用入口对照(我们的适配器)

| 我们的 kind | 适配器文件 | 对接的源 |
|---|---|---|
| `netease-api`(builtin) | `neteaseApi.ts` | 前端直连 music.163.com |
| `netease-api`(external) | `neteaseApi.ts` | 自部署 NeteaseCloudMusicApi / api-enhanced |
| `cyrene-aggregate`(omni) | `cyreneApi.ts` | OmniParse |
| `cyrene-aggregate`(tunehub) | `cyreneApi.ts` | TuneHub |
| `cyrene-aggregate`(lx) | `cyreneApi.ts` | 洛雪自定义直链源(模板) |
| `lx-server` | `lxServer.ts` + `discovery.ts` | lxserver 增强 fork 的 REST `/api/music/*`(见第 4 节) |
| `plugin-js` | `pluginAdapter.ts` | MusicFree/LX JS 插件 |
| `aggregate-http` | `aggregate.ts` | 通用模板化 HTTP 源 |
| `local` | `localMusic.ts` | 本地文件 |

> ✅ 已核实:`lxServer.ts` 调的 `/api/music/*` 就是用户部署的 **lxserver 增强 fork**(`D:\…\Rust\lxserver`,v1.9.3)对外暴露的接口。该 fork = 官方 lx-music-sync-server 同步服务 + web 播放器 + 完整 `/api/music/*` REST 音源 + Subsonic API + vm2 沙箱跑洛雪脚本,五合一。全部接口见第 4 节(源码逐行核实)。

## 1. 网易云 API(NeteaseCloudMusicApi 377 / api-enhanced 399)

路由规则:文件名去 `.js`、`_`→`/`(`song_url_v1`→`/song/url/v1`);例外 `/daily_signin`/`/fm_trash`/`/personal_fm`。`app.all` GET/POST 皆可。匿名 token 自动注入。

### 1.1 我们已调用(✅)

> 落点行号随代码演进会漂移,以函数名为准。builtin = 前端直连 music.163.com(weapi/eapi 加密 + 匿名 token,移到 Rust `netease.rs`);external = 自部署 NCM/enhanced。

| 路由 | 我们的落点(builtin / external) | 用途 / 函数 |
|---|---|---|
| `/cloudsearch` `type=1/100/1000` | external;builtin 退化 `/api/search/get` | 搜歌/歌手/歌单 `searchNeteaseApi`/`searchNeteaseArtists`/`searchNeteasePlaylists` |
| `/song/url/v1?id=&level=` | external;builtin weapi `song/enhance/player/url/v1` | 播放直链 `resolveNeteaseApi` |
| `/song/url/v1/302` ⭐ | external 独占 | v1 取不到链时回落 302 直挂 `getNeteaseSongUrl302` |
| `/song/url/match?id=&source=` ⭐ | external 独占 | 灰曲解灰第三方补链 `resolveNeteaseUnblockMatch` |
| `/song/copyright/rcmd?id=` ⭐ | external 独占 | 灰曲换可播版本(解灰链路第 3 步)`getNeteaseSongCopyrightRcmd` |
| `/lyric/new` | external;builtin `/api/song/lyric/v1` | 逐字歌词 `fetchNeteaseLyric` |
| `/lyric`(整轨) | external;builtin `/api/song/lyric` | 逐字空时回退纯歌词 `getNeteaseLyricPlain` |
| `/comment/music` | external;builtin `/api/v1/resource/comments` | 评论 `getNeteaseComments` |
| `/simi/song` | external;builtin `/api/v1/discovery/simiSong` | 相似歌曲 `getNeteaseSimiSongs` |
| `/simi/artist` | external | 相似歌手(真接口)`getNeteaseSimiArtists` |
| `/personalized` | external;builtin `/api/personalized/playlist` | 推荐歌单 `getNeteasePersonalized` |
| `/personalized/newsong` | both | 推荐新歌 `getNeteaseNewSongRecommend` |
| `/personalized/mv` | both | MV 列表 `getNeteaseMvList` |
| `/personalized/djprogram` | external | 精选电台节目 `getNeteaseDjProgramRecommend` |
| `/toplist` | both | 排行榜列表 `getNeteaseToplists` |
| `/toplist/detail` | external | 一次拿全榜单+摘要 `getNeteaseToplistDetail`(发现页聚合优先) |
| `/playlist/track/all` | external;builtin `/api/v6/playlist/detail` | 歌单内歌曲 `getNeteasePlaylistSongs` |
| `/top/playlist` `/top/playlist/highquality` `/playlist/catlist` | external | 歌单广场(分类/精品/标签)`getNeteaseTopPlaylists`/`getNeteaseHighqualityPlaylists`/`getNeteasePlaylistCatlist`/`getNeteaseHotPlaylistTags` |
| `/album/new` `/album/newest` `/top/album` | external | 新碟上架页 `getNeteaseNewAlbums`/`getNeteaseNewestAlbums`/`getNeteaseTopAlbums` |
| `/artists` `/artist/album` `/artist/list` `/top/artists` | external(builtin 多 -462) | 歌手页 `getNeteaseArtist`/`getNeteaseArtistAlbums`/`getNeteaseArtistList`/`getNeteaseTopArtists` |
| `/artist/top/song` `/artist/songs` `/artist/desc` | external | 歌手热门/全部歌/简介 `getNeteaseArtistTopSongs`/`getNeteaseArtistAllSongs`/`getNeteaseArtistDesc` |
| `/album` | external;builtin `/api/v1/album/{id}` | 专辑详情 `getNeteaseAlbum` |
| `/song/detail?ids=` | external;builtin weapi `v3/song/detail` | 批量元信息回填 `getNeteaseSongDetail`(内部解灰链路用) |
| `/song/wiki/summary` | external | 歌曲百科 `getNeteaseSongWiki`(歌曲详情面板) |
| `/check/music?id=&br=` | external | 播放前可用性预检 `checkNeteaseMusicAvailable`(已实现,未挂 UI) |
| `/mv/url` | both | MV 直链 `getNeteaseMvUrl` |
| `/mv/detail` `/mv/first` `/top/mv` | both/external | MV 详情/最新/榜 `getNeteaseMvDetail`/`getNeteaseFirstMv`/`getNeteaseTopMv` |
| `/dj/recommend` `/dj/program` | both | 电台 `getNeteaseRadioRecommend`/`getNeteaseRadioPrograms` |
| `/dj/hot` `/dj/toplist` `/dj/banner` | external | 电台扩展(热门/榜单/banner)`getNeteaseDjHot`/`getNeteaseDjToplist`/`getNeteaseDjBanner` |
| `/search/hot/detail` | external;builtin `/api/search/hot` | 热搜词 `getNeteaseHotSearch` |
| `/search/suggest` `/search/suggest/pc` ⭐ | external;builtin weapi | 搜索框补全 `getNeteaseSearchSuggest` |
| `/search/default` | external | 默认搜索词(占位)`getNeteaseDefaultKeyword` |
| `/banner?type=0` | external;builtin `/api/v2/banner/get` | 首页轮播图 `getNeteaseBanners` |
| `/homepage/block/page` | external;builtin `/api/homepage/block/page` | 发现页首屏分块 `getNeteaseHomepageBlocks` |

### 1.2 未调用(⬜)—— 仅剩登录态接口

> 06-21 文档列的发现页/歌手/电台/搜索补全/解灰/302/check/banner/homepage 等扩展接口本轮已全部接入(见 1.1)。**剩下未接的全部需要登录 cookie**,匿名拿不到,留待登录态功能开放后再做。

| 路由 | 价值 | 备注 |
|---|---|---|
| `/recommend/songs` `/recommend/resource` 🔑 | 每日推荐 / 每日歌单 | 需登录 |
| `/personal_fm` 🔑 | 私人 FM | 需登录 |
| `/like` `/likelist` 🔑 | 红心同步 | 需登录 |
| `/comment/hot` `/comment/new` `/comment/floor` | 评论扩展(楼层/最新) | 低价值,现有 `/comment/music` 已够 |

## 2. OmniParse(bun+elysiajs `:4055`)

> ✅ **基于源码逐行核实**(`src/index.ts` 路由注册)。实际暴露 **19 个路由**(健康检查 + 音乐 7 + B站 10 + 抖音 1 + 版本 1 + MPD 静态),不止搜索+直链。CORS 全开,无鉴权(cookie 走服务端本地文件 `cookie/`)。定位是「初音猫音乐」App 后端(`Chuxin-Neko/chuxinneko_music`)。
> ⚠️ **架构关键**:**只有 netease / qq / kugou 三个音乐平台,没有 `/kuwo/*` 路由!** 见 2.3 的我们调用 bug。

### 2.1 全部路由(源码确证;★=我们已调)

| 路由 | method | 参数 | 返回 / 说明 | 我们 |
|---|---|---|---|---|
| `/` | GET | — | `"OK"` 健康检查 | ⬜ |
| `/search` | POST(urlencoded) | `keywords`(必), `limit` | 网易搜索 `{status,result:[{id,name,artists,album,picUrl}]}` | ★`cyreneApi.ts:32` |
| `/song` | POST | `ids`/`url`(二选一必), `level`(必), `type`(必:json/down/text) | 网易直链。json→`{name,pic,ar_name,al_name,level,size,url,lyric,tlyric}`;down→302 跳直链;text→HTML 文本 | ★`buildOmniRequest`(POST,已修;歌词一并取)|
| `/toplists` | GET | — | 网易 4 内置榜单(飙升/新歌/原创/热歌),每榜补全 20 首 | ★`getOmniToplists`(发现页聚合)|
| `/qq/search` | GET | `keywords`(必), `limit`(默20) | `{status,result:[{name,album,singer,pic,mid,id}]}` | ★`:33` |
| `/qq/song` | GET | `url`/`ids`(二选一必) | `{song,lyric:{lyric,tylyric},music_urls:{flac,320,128}}` | ★`:184` |
| `/kugou/search` | GET | `keywords`(必), `limit`(默20) | `{status,result:[{name,hash,album,singer,duration,emixsongid,pic}]}` | ★`:35` |
| `/kugou/song` | GET | **`emixsongid`(必)** | `{status,song:{name,singer,album,pic,lyric,url,bitrate,duration}}` | ★`buildOmniRequest`(emixsongid,已修)|
| `/bili/ranking` | GET | `rid`(默0), `type`(默all) | B站排行榜 v2 原始 JSON | ⬜ |
| `/bili/cid` | GET | `bvid`(必) | `{code,message,cid}` | ⬜ |
| `/bili/playurl` | GET | `bvid`(必), `format`(可:mpd) | DASH 播放数据(含 `dash.audio[]` 音轨);`format=mpd` 生成 .mpd 返回 url | ⬜ |
| `/bili/pgc_season` | GET | `ep_id`/`season_id` | B站番剧详情 | ⬜ |
| `/bili/pgc_playurl` | GET | `ep_id`/`cid`, `format`(mpd) | 番剧 DASH/mpd | ⬜ |
| `/bili/danmaku` | GET | `oid`(cid,必), `segment_index`(必) | protobuf 解析后弹幕 | ⬜ |
| `/bili/search` | GET | `keyword`(必) | B站综合搜索 v2 | ⬜ |
| `/bili/comments` | GET | `type`(必), `oid`(必), `mode`, `pagination_str` | B站评论 | ⬜ |
| `/bili/proxy` | GET | `url`(必), 透传 Range | 流式回源(注入 Referer/UA/Cookie),供 MPD BaseURL 回源 | ⬜ |
| `/douyin` | GET | `url`(必,分享链接/文本) | `{code,data:{...,results:[{aweme_id,desc,author,video,images,music:{url}}]}}` | ⬜ |
| `/version/latest` | GET | — | 硬编码 App 版本信息 | ⬜ |
| `/mpd/:filename` | GET(静态) | 文件名 | `temp_mpd/` 下生成的 DASH 清单 | ⬜ |

### 2.2 已接入(★)与未接(⬜)

| 路由/能力 | 状态 | 落点 |
|---|---|---|
| `GET /toplists` | ★ 已接 | `getOmniToplists` → 发现页榜单聚合(`discoveryAggregate` cyrene 分支,歌曲内联缓存) |
| `/song` 返回的 `lyric`+`tlyric` | ★ 已接 | omni 播放时一并取出存歌词(`resolveCyrene`),网易缺词再回退 `getOmniNeteaseLyric` |
| `GET /douyin` 的 `results[].music.url` | ★ 已接 | `getOmniDouyinMusic`/`getOmniDouyinSongs` → 搜索框粘贴抖音链接即解析 BGM 入队 |
| QQ `/qq/song` 的 `music_urls.{flac,320,128}` 三档 | ⬜ | 解析时按 quality 选档(现在取默认档) |
| `/song` 的 `type=down`(302)/`type=text` | ⬜ | 302 直挂下载 / 文本展示 |
| `GET /bili/search`→`/bili/playurl`→`/bili/proxy` | ⬜ | 新增 bilibili 音源(强依赖 cookie,音轨必须经 `/bili/proxy`) |
| 整条 B 站链(ranking/cid/pgc/danmaku/comments) | ⬜ | 与音乐模块无关,视频模块若要可复用 |

### 2.3 ✅ 已修的调用 bug(`cyreneApi.ts`,2026-06 核验)

- ~~`/kuwo/*` 404~~ → 已删酷我端点:omni 平台收敛为 `wy/tx/kg`(`DEFAULT_PLATFORMS`、`SEARCH_SPECS` 仅三平台)。
- ~~`/kugou/song` 传错参~~ → 已改传 `emixsongid`(搜索结果的 `emixsongid` 字段;`buildOmniRequest` kg 分支)。
- ~~netease `/song` 用 GET `?id=&quality=`~~ → **已改 POST** `{ids,level,type}`(对齐 `index.ts` 源码,`buildOmniRequest` wy 分支);歌词随播放一并取出,不再额外请求。

> OmniParse 的 cookie 走服务端本地文件(`cookie/`),我们前端不用管。`.cyrene` 配置文件解密我们已实现(`cyreneConfig.ts`),解出 `{name,url,apiKey}`。

## 3. UnblockNeteaseMusic(`server`)

### 3.1 我们已调用

**无。** 目前没有任何代码对接 UNM。

### 3.2 接口形态(⬜ 全部未用,源码确证)

入口 `app.js` 逐行核实:UNM 启动的是 **http/https 双端口 MITM 代理服务器**(`server.http`/`server.https` + `hook` 拦截 + 白/黑名单 + endpoint 替换),**没有任何 REST 路由**。对外编程面只有两个:

- **MITM 代理**(主用法):改 hosts/PAC 或设系统代理,拦截 `music.163.com` 的 `song/enhance/player/url` 返回,把灰曲链替换成其他平台直链。CLI:`-p 端口` `-o 音源顺序` `-e endpoint` `-u 上游代理` `-s 严格模式` `-t 认证token` `-c 大陆中转`。**不适合我们**(我们是独立客户端,不走系统代理拦截网易请求)。
- **库函数** `match(id, source[], data)`(`provider/match.js`)→ `{url,br,size,md5,source}`。要用必须自己包一层 HTTP 服务(Node 进程内 `require` 调用)。

### 3.3 各 provider 真实能力(`src/provider/*.js`,统一 `check(info)` 模式)

每个 provider 导出 `check(info)`:内部 `search(info)`(按 `歌名 - 歌手` 关键词搜上游)→ `select()`(匹配最佳)→ `single()`(取直链)。`info` 来自 `find.js`(网易 `song/detail` 拿到的 `{name,artists,album,keyword,duration}`)。

| 代号 | 平台 | 默认启用 | cookie/依赖 | 备注 |
|---|---|---|---|---|
| `qq` | QQ 音乐 | | `QQ_COOKIE`(必,uin+qm_keyst) | 必须 QQ 登录 |
| `kugou` | 酷狗 | ✅ | 无 | 关键词搜→hash 取链 |
| `kuwo` | 酷我 | | 无(`KUWO_COOKIE` 可选) | `search.kuwo.cn/r.s` 关键词搜 |
| `bodian` | 波点 | ✅ | 无 | |
| `migu` | 咪咕 | ✅ | `MIGU_COOKIE`(aversionid) | |
| `joox` | JOOX | | `JOOX_COOKIE`(wmid+session_key) | 地区限制严 |
| `youtube` | YouTube(纯JS) | | `YOUTUBE_KEY`(Data API v3) + 非大陆 IP | |
| `youtubedl` | YouTube(youtube-dl) | | 装 youtube-dl | |
| `ytdlp` | YouTube(yt-dlp) | ✅ | 装 yt-dlp | |
| `bilibili` | B站音频区 | | 无 | |
| `bilivideo` | B站视频 | | 无 | 大陆外 IP 部分版权视频查不到 |
| `pyncmd` | 第三方 NCM(GD音乐台) | | 无 | `music.gdstudio.xyz` 提供 |

环境变量影响匹配:`ENABLE_FLAC`(无损)、`SELECT_MAX_BR`(选所有源最高码率)、`FOLLOW_SOURCE_ORDER`(严格按 `-o` 顺序)、`MIN_BR`(低于则替换)、`SEARCH_ALBUM`(搜索带专辑名)、`NETEASE_COOKIE`、`ENABLE_LOCAL_VIP`(本地黑胶 VIP)。

### 3.4 结论(对我们项目)

| 能用在哪 | 怎么用 |
|---|---|
| 网易灰曲兜底 | 自建 wrapper 调 `match()`,或 **直接用 lxserver/enhanced 的 `/song/url/match`(等价能力,免自建)** |

> UNM 的核心价值(网易灰曲换其他平台直链)被 **enhanced API 的 `/song/url/match`** 完全覆盖。除非要 `youtube`/`bilivideo` 这类 UNM 独有上游,否则优先用 enhanced 解灰,**不必自建 UNM wrapper**。若真要接,因 UNM 无 REST,得起一个 Node 服务把 `match()` 包成 HTTP,成本高于直接用 enhanced。

## 4. lxserver(增强 fork v1.9.3,源码 `D:\…\Rust\lxserver`,部署 `http://35.208.239.12:9527`)

> ✅ **基于源码逐行核实**(`src/server/server.ts` 5678 行)。用户给的 `D:\…\Rust\lxserver` 就是这台部署的源码 —— 它是把【官方 lx-music-sync-server 同步服务】+【web 播放器】+【完整 `/api/music/*` REST 音源】+【Subsonic 兼容 API】+【vm2 沙箱跑洛雪自定义源脚本】五合一的增强版。**我们 `lx-server` kind 适配器对接的 `/api/music/*` 就是这套**,即第 5 节那个"lx-music-api-server"实际等同本节。
>
> 支持音源(`musicSdk/`):`wy`网易 `tx`QQ `kg`酷狗 `kw`酷我 `mg`咪咕 `bd`百度。
> ⚠️ **架构关键**:搜索/歌词/榜单/歌单/歌手/专辑/热搜/评论走**内置 musicSdk**(免登可用);**`/api/music/url` 播放直链只走 vm2 里加载的洛雪自定义源脚本**(内置 SDK 无独立解析,无脚本则报"未找到支持 X 平台的自定义源")。这就是"web 播放器解析落雪脚本"的实现。

### 4.1 音乐接口 `/api/music/*`(源码确证;★=我们已调)

| 路由 | method | 参数 | 返回 | 我们 |
|---|---|---|---|---|
| `/api/music/search` | GET | `name`(必), `source`(默kw), `type`(song/singer/album/playlist), `page`, `limit`, `pages`(一次多页) | 歌曲数组 | ★`searchLxServer`(type=song)+`searchLxArtist`(type=singer)|
| `/api/music/url` | POST | `{songInfo,quality,enableAutoSwitchApiSource}` | `{url,type,sourceName,attempts[]}` | ★`resolveLxServer` |
| `/api/music/lyric` | GET/POST | GET:`id`,`source`;POST:`{songInfo}` | `{lyric,tlyric,rlyric,lxlyric}` | ★`fetchLxLyric` |
| `/api/music/tipSearch` | GET | `name`,`source` | 搜索建议数组 | ★`getLxTipSearch`(搜索补全)|
| `/api/music/hotSearch` | GET | `source`(默mg) | `{source,list[]}` | ★`getMusicHotSearch` |
| `/api/music/songList/tags` | GET | `source` | `{tags,sortList}` | ★`getAllMusicSonglistTags` |
| `/api/music/songList/list` | GET | `source`,`tagId`,`sortId`,`page` | 歌单广场 | ★`getAllMusicSonglists` |
| `/api/music/songList/detail` | GET | `source`,`id`,`page` | 歌单歌曲 | ★`getMusicSonglistDetail` |
| `/api/music/songList/search` | GET | `source`,`text`,`page` | 歌单搜索 | ★`searchLxSonglists`(搜索页歌单 tab)|
| `/api/music/songList/userPlaylist` | GET | `source`(默tx),`uid`,`page` | 用户歌单 | ⬜ 无自然入口(需 uid,要专门导入 UI)|
| `/api/music/leaderboard/boards` | GET | `source`(默kg) | `{list:[{id,name,bangid}]}` | ★`getMusicBoards` |
| `/api/music/leaderboard/list` | GET | `source`,`bangid`,`page` | 榜单歌曲 | ★`getMusicBoardSongs` |
| `/api/music/artistDetail` | GET | `id`,`source`(默wy) | 歌手详情 | ★`getLxArtistDetail`(歌手页 LX 回退)|
| `/api/music/artistAlbums` | GET | `id`,`source`,`page` | 歌手专辑 | ★`getLxArtistAlbums` |
| `/api/music/artistSongs` | GET | `id`,`source`,`order`(hot/time) | 歌手全部歌(后端循环拉≤500) | ★`getLxArtistSongs` |
| `/api/music/albumSongs` | GET | `id`,`source` | 专辑歌曲 | ★`getLxAlbumSongs`(专辑页 LX 路由)|
| `/api/music/comment` | POST | `{songInfo,type(hot/normal),page,limit}` | `{comments[]}` | ★`getLxComments`(播放页评论 tab)|
| `/api/music/progress` | GET(SSE) | `reqId` | 解析进度推送(配合 url 的 `x-req-id` 头) | ⬜ 可接(播放 loading)|
| `/api/music/identify` | POST | 听歌识曲 | — | ⬜ |
| `/api/music/config` | GET | — | `{player.enableAuth,user.enablePublicRestriction}` | ⬜ |

### 4.2 服务端缓存/下载(播放器自带的服务端缓存,我们用不上但可了解)

`/api/music/cache/{config,sync,subdirs,mkdir,categorize,rename,check,download,stop,file/*,stats,clear,lyric/clear,lyric,progress,list,cover,remove,move,switch-base,updateMetadata,embedLyric,link}`、`/api/music/download`(GET 流式下载)。多数需 `x-user-token`。

### 4.3 用户/歌单/收藏(具名用户态,需 `x-user-token`)

- 登录:`POST /api/user/login`(`{username,password}`→`{token}`)、`/api/user/logout`、`GET /api/user/auth/verify`、`POST /api/user/verify`。
- 歌单:`GET/POST /api/user/list`(全量读/覆盖)、`POST /api/music/user/list/add`、`/api/music/user/list/remove`(批量增删)。
- 收藏库:`GET/POST /api/user/library/artists`、`/api/user/library/albums`(收藏歌手/专辑,完整覆盖)。
- 设置/音效:`GET/POST /api/user/settings`、`/api/user/sound-effects`。
- API Token 管理:`/api/user/token/{config,add,remove,update,toggle,logs}`。
- ⬜ 这些可用于"登录后跨端同步收藏歌手/专辑/歌单/设置"。

### 4.4 自定义源(洛雪脚本)管理 `/api/custom-source/*`

`validate`(校验脚本)、`import`(导入)、`upload`(上传文件)、`list`(列出)、`toggle`(启停)、`delete`、`reorder`。脚本在 **vm2 沙箱**执行(`userApi.ts`),`/api/music/url` 靠这些脚本解析直链。⬜ 我们可加"上传洛雪脚本到服务器"入口。

### 4.5 Subsonic 兼容 API `/rest/*`(默认可开,标准 Subsonic 协议)

`subsonic.ts` 实现:`ping getLicense getMusicFolders getMusicDirectory getGenres getArtists getArtist getArtistInfo(2) getAlbum getAlbumList(2) getSong getRandomSongs getSongsByGenre getSimilarSongs(2) getTopSongs search(2/3) getPlaylists getPlaylist updatePlaylist getStarred(2) stream download getCoverArt getLyrics getLyricsBySongId getInternetRadioStations getNowPlaying scrobble getScanStatus getOpenSubsonicExtensions`。⬜ **意味着任何 Subsonic 客户端都能直连这台播放音乐** —— 我们若想省事,也可走 Subsonic 协议接(但当前 lx-server 适配器已够用)。

### 4.6 管理/运维(需 `x-frontend-auth` 管理员密码)

`/api/login /api/status /api/users(增删改查) /api/data(+snapshots/snapshot/restore-snapshot/delete-snapshot/upload-snapshot/delete-playlist/delete-song/rename-playlist/batch-delete-songs) /api/config(+test-proxy) /api/logs /api/stats /api/webdav/*(test/sync/backup/restore/logs/progress) /api/backup/{download,upload} /api/admin/{verify,reload} /api/restart /api/files(增删改查+download) /api/elfinder/connector(文件管理器)`。与音乐无关,运维用。

### 4.7 WebSocket 歌单同步(官方原有,洛雪客户端收藏同步)

- HTTP 握手:`GET /hello`(`Hello~::^-^::~v4~`)、`/id`(serverId)、`/ah`(authCode)。
- 鉴权:连接码 → `MD5(pwd).slice(0,16)` base64 作 AES key → 解握手 → RSA 回传 `{clientId,key,serverName}`;WS 升级 `?i=&t=`。
- WS 动作:`list_*` / `dislike_*` 同步(见 `modules/list/sync`)。
- ⬜ 我们未接;**作为音源用不需要碰它**(走 4.1 即可),仅"洛雪 App 收藏跨端同步"才需要。

### 4.8 结论(对我们项目)

1. **这台服务器现在就能直接当 LX Music API Server 用**:设置 → 音乐管理 → 添加 LX Music API Server → 填 `http://35.208.239.12:9527`。我们 `lx-server` 适配器开箱即用(搜索/播放/歌词/榜单/歌单/标签/热搜全通)。`player.enableAuth` 只挡 web 播放器页面,`/api/music/*` 接口本身匿名可用(实测歌词/榜单/歌单都通)。
2. **扩展接口现已全部接入**:`tipSearch`(搜索补全)、`artistDetail/artistAlbums/artistSongs`(歌手页 LX 回退,经 `searchLxArtist` 按名解析歌手 id)、`albumSongs`(专辑页 LX 路由,歌曲带 albumId 即可点专辑名)、`comment`(播放页评论 tab)、`songList/search`(搜索页歌单 tab)均已对接。**仅剩** `userPlaylist`(需 uid,无自然入口)、`progress`(SSE 播放进度,非必需)未接。
3. `/api/music/url` 能否出链**取决于服务器上装了哪些洛雪自定义源脚本**(`attempts` 里能看到走了哪个源)。这是该服务的命门,与我们无关。

## 5.(已并入第 4 节)

> 之前推测的"lx-music-api-server 是另一个项目"已更正:用户部署的 lxserver fork 自带 `/api/music/*`,就是我们 `lxServer.ts`/`discovery.ts` 对接的同一套接口。覆盖见 4.1 的 ★ 项 —— 搜索/播放/歌词/榜单/歌单/标签/热搜/歌手/专辑/评论/搜索补全均已调用,`buildLxMusicStreamUrl` 还包了本地稳定流代理。**核心播放链路 + 扩展页面全部无缺口**,仅剩 `userPlaylist`(无入口)、`progress`(SSE,非必需)未接。

## 6. TuneHub V3(闭源商业服务 `https://tunehub.sayqz.com/api`,代理抓到完整文档)

> 定位:**高性能音乐解析中间件,credit 计费**。支持平台:**netease / qq / kuwo**(仅 3 个)。
> 鉴权:请求头 `X-API-Key: th_xxx`(除公开接口外都要)。base URL = `https://tunehub.sayqz.com/api`。

### 6.1 ✅ 已修(原 `?type=url` bug)

- **原 bug**:旧实现 `GET {base}/api/?type=url&source=netease&id=&br=` 返 404(V3 无此接口)。
- **现状**:`cyreneApi.ts` 的 `resolveTunehub` 已改为 `POST {base}/v1/parse` + 头 `X-API-Key`(用 `source.token` 存 key),body `{platform,ids:song.id,quality}`,从 `data.data[0].url` 取链,顺带存 `actualQuality/lyrics`。平台映射 `TUNEHUB_SOURCE`(wy→netease/tx→qq/kw→kuwo),音质 `tunehubQuality`(128k/320k/flac/flac24bit,192k 归 320k)。真实接口见 6.2。

### 6.2 真实接口全集(代理 curl 逐个实测确证)

**A. 核心解析 `/v1/parse`(消耗积分,需 key)**

| 路由 | method | 请求体/参数 | 返回 |
|---|---|---|---|
| `/api/v1/parse` | POST(JSON) + 头 `X-API-Key` | `{platform:"netease\|qq\|kuwo", ids:"id1,id2", quality:"128k\|320k\|flac\|flac24bit"}` | `{code,data:{total,success_count,fail_count,cache_hit_count,cost,data:[{id,success,fromCache,info:{name,artist,album,duration},cover,url,actualQuality,wasDowngraded,fileSize,lyrics}]}}` |

- 实测:无 key → `{"code":401,"message":"Missing API Key"}`;假 key → `{"code":401,"message":"Invalid API Key"}`。**必须有效 key 才能取直链。**
- `ids` 批量逗号分隔;`quality` flac24bit 仅 netease/qq(kuwo 不支持)。返回一次拿全 url+cover+lyrics+元信息。

**B. 方法下发 `/v1/methods`(不消耗积分,公开免 key,实测可用)**

| 路由 | method | 实测返回 |
|---|---|---|
| `/api/v1/methods` | GET | `{data:{kuwo:[search,toplists,toplist,playlist], qq:[...], netease:[...]}}` 三平台各 4 方法 |
| `/api/v1/methods/:platform` | GET | `{data:{platform,availableMethods:[...]}}` |
| `/api/v1/methods/:platform/:function` | GET | 上游请求配置 `{type:"http",method,url,params/body(含{{变量}}),headers,transform(JS函数串)}` |

**实测拿到的真实上游配置**(客户端拿到后替换 `{{变量}}` 自己请求上游,再用 `transform` 解析):

- `netease/search` → `GET music.163.com/api/search/get/web`,params `{s:{{keyword}}, type:1, offset:{{((page||1)-1)*(limit||20)}}, limit:{{limit||20}}}`,transform 取 `result.songs[]`→`{id,name,artist,album}`。
- `netease/toplists` → `GET music.163.com/api/toplist`,transform 取 `list[]`→`{id,name,pic,updateFrequency}`。
- `netease/toplist` & `netease/playlist` → `GET music.163.com/api/playlist/detail`,params `{id:{{id}}, n:100000, s:8}`;playlist 额外返回 `info{name,pic,desc,author,playCount}`。
- `qq/search` → **POST** `u.y.qq.com/cgi-bin/musicu.fcg`,body 是 `music.search.SearchCgiService` 的 JSON,query=`{{keyword}}`,transform 取 `req.data.body.song.list[]`→`{id:mid,name,artist,album}`。
- ⚠️ `kuwo/*` 的 methods 列在清单里,但实测单独取配置**返回空**(上游配置可能缺失/超时)——kuwo 方法下发不可靠。

> 模式总结:**搜索/榜单/歌单 = 方法下发,不扣积分,但客户端要会 eval `transform` JS 串 + 自己跨域请求上游**(浏览器端有 CORS 风险,文档明说要服务端/代理转发);**取播放直链 = `/v1/parse`,扣积分,需 key**。

**错误码**:`0` 成功 / `-1` 通用 / `-2` 积分不足 / `401` Key 缺失/无效 / `403` 封禁/Key 禁用 / `404` 资源不存在 / `500` 服务器错误。

**管理后台**(SPA 有 login/keys/users/credits/logs 页,对应后端端点实测未直接命中,非音乐对接所需,略)。

### 6.3 现状与遗留

- ✅ **播放已修**:`POST /v1/parse` + `X-API-Key`,body `{platform,ids,quality}`,取 `data.data[0].url`(见 6.1)。platform 仅 netease/qq/kuwo;quality 传 `128k/320k/flac/flac24bit`。
- **搜索仍走 OmniParse 端点**(`SEARCH_SPECS`),与 tunehub 无关 —— 这是有意为之:tunehub 源只做播放解析,搜索由聚合里其他源承担。真用 tunehub 搜索需走方法下发(`GET /v1/methods/:platform/search` → eval transform → 请求上游),复杂度高、收益低,**暂不做**。
- **注意**:付费服务,需用户自备 `X-API-Key`(UI 导入 tunehub 源时填到 `source.token`)。kuwo 方法下发不可靠,优先 netease/qq。

---

## 7. 总结:现状与遗留(2026-06 复核后更新)

**已覆盖(基本全接)**:
- **网易**:搜索/播放/逐字歌词/评论/相似/推荐;解灰三件套(`/song/url/match`+`/song/copyright/rcmd`+UNM 移植);302 直挂、可用性预检、搜索补全(suggest/default);发现页全套(`/toplist/detail`、`/top/playlist`+highquality+catlist、`/album/new`+newest+top、`/banner`、`/homepage/block/page`);歌手页全套(`/artists`、`/artist/songs`+top/song、`/simi/artist`、`/artist/desc`);电台四档、MV 三档、歌曲百科、批量详情。builtin 走 weapi/eapi+匿名注入(Rust 后端),external 走自部署 NCM/enhanced。
- **LX**:搜索/播放/歌词/榜单/歌单/标签/热搜 + 歌手搜→详情/专辑/歌曲、专辑歌曲、歌单搜、tipSearch、评论。核心播放链路 + 扩展无缺口。
- **OmniParse**:搜索(wy/tx/kg)、播放(netease POST `/song` 已修、qq、kugou emixsongid 已修)、`/toplists`(发现页聚合)、`/douyin`(抖音 BGM,粘贴分享链即播)。
- **TuneHub**:`POST /v1/parse`+`X-API-Key` 播放解析(已修,原 `?type=url` 是坏的)。

**2026-06 复核修的 3 个 bug**:① OmniParse netease `/song` GET→POST `{ids,level,type}`;② `getNeteaseSongDetail` builtin `c` 参数改数字 id;③ `getNeteaseDjCategoryRecommend` 改名 `getNeteaseDjProgramRecommend`(名实不符,端点本就是 `/personalized/djprogram`)。

**有意不做(收益低/需登录/越界)**:
- 网易登录态 🔑:`/recommend/songs`(每日推荐)、`/personal_fm`(私人 FM)、`/like`+`/likelist`(红心同步) —— 需扫码/cookie 登录态,当前匿名架构不支持。
- `/check/music` 预检、`/song/detail` 批量回填、`/mv/detail`、LX `/api/music/songList/userPlaylist`(用户歌单,需 uid 无入口) —— 功能冗余或无自然入口,接了是为用而用。
- **OmniParse B 站音源**(`/bili/search`→`/bili/playurl`→`/bili/proxy`)—— 强依赖 cookie + Agora/DASH,属视频模块范畴。
- **UNM** —— 核心能力(灰曲换链)已被 enhanced `/song/url/match` + 我们移植的 Rust `music_unblock` 覆盖,无需自建 Node wrapper。
- **lxserver Subsonic / WebSocket 歌单同步 / 服务端缓存 / 运维接口** —— 作音源播放不需要;仅"洛雪 App 收藏跨端同步"才需 WS 握手(AES/RSA),工作量中等,暂不做。

> **未做运行时验证**:本轮所有改动均 `tsc` 编译期通过,但 LX 歌手/专辑/评论、网易首页分块、抖音 BGM、OmniParse netease POST `/song` 的真实数据形态需 `pnpm tauri dev` 连真实后端跑一遍确认归一字段对得上。

---

## 8. 附:接入你这台 lxserver(`35.208.239.12:9527`)的实操

1. 设置 → 音乐管理 → 音乐源 → "添加 LX Music API Server"。
2. 地址填 `http://35.208.239.12:9527`(**不带** `/music`,`/api/music/*` 在根路径)。Token 留空先试;若 `/api/music/search` 返回空,大概率是服务端开了 `user.enablePublicRestriction`,需登录拿 token 后填到 Token 框(我们适配器把 token 放进 `x-user-token` 头,见 `lxServer.ts:47`)。
3. 启用并设为活跃源。搜索 / 播放 / 歌词 / 榜单 / 歌单 / 热搜应即时可用。
4. 播放能否出链取决于服务器上装的洛雪自定义源脚本(响应 `attempts[]` 可见走了哪个源)。这是服务端配置,与我们无关。

> 待核实项:实测匿名 `/api/music/search?name=晴天&source=kw` 返回空数组,而 `/lyric`、`/hotSearch`、`/leaderboard`、`/songList` 均正常。需确认 search 是否要登录态 —— 接入时先观察,必要时加 token。



